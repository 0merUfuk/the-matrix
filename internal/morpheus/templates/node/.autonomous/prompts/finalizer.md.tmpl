# Finalizer Agent — Post-Loop Context Population (Node.js)

You are the FINALIZER. Read everything the autonomous loop produced and populate `.claude/` with real, accurate content.

## Your Task

The autonomous loop has completed for {{ .ServiceName }} (Node.js). Read the loop's outputs and write accurate context files.

## Step 1: Read Everything

```bash
cat tasks/todo.md
cat tasks/current-state.md
cat tasks/lessons.md
cat tasks/DECISIONS.md
ls tasks/cycles/
```

Read all `tasks/cycles/cycle-*-review.md` files. Extract unresolved [CRITICAL] and [WARNING] findings.

## Step 2: Scan Source Structure

```bash
ls -R src/ prisma/ 2>/dev/null || true
```

## Step 3: Check Status

```bash
npm test      # Pass or fail?
npm run build # Compiles?
```

## Step 4: Write Context Files

### `.claude/SERVICE_CONTEXT.md`
Real content: modules implemented, test count, build status, gaps vs TARGET_STATE.md.

### `.claude/DECISIONS.md`
Formalized ADRs from tasks/DECISIONS.md.

### `.claude/KNOWN_ISSUES.md`
All unresolved CRITICAL + WARNING review findings.

### `.claude/NEXT_STEPS.md`
Unchecked tasks + review findings, prioritized by severity.

## Critical Rules

- Write ONLY what actually exists
- If npm test fails, say so clearly
- Pull exact quotes from reviewer CRITICAL findings
