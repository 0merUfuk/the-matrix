**Version**: 1.0
**Created**: {{ .CreatedDate }}
**Last Updated**: {{ .CreatedDate }}
**Authors:** Autonomous Loop

---

# {{ .ServiceNamePascal }} — Next Steps

> **Placeholder** — Generated by morp-cli on {{ .CreatedDate }}.
> Run `morp finalize` after the autonomous loop completes to replace with real content.

## Current Status

NOT STARTED — Scaffolded on {{ .CreatedDate }}. Autonomous loop has not run yet.

---

## High Priority

### 1. Run the Autonomous Development Loop

```bash
bash .autonomous/loop.sh
```

The loop will implement all tasks in `tasks/todo.md` across {{ .MaxCycles }} cycles
(Developer → Tester → Reviewer per cycle).

**Estimated cycles**: {{ .MaxCycles }}
**Entities to implement**: {{ join .Entities ", " }}

---

## Medium Priority

### 2. Review Pre-filled Architectural Decisions

Read `tasks/DECISIONS.md` — all wizard answers are pre-filled as ADRs. Verify they match
your requirements before the loop runs.

### 3. Review AI-Generated Task List

Read `tasks/todo.md` — add or remove tasks if the AI-generated breakdown is missing
service-specific requirements.

---

## Low Priority

### 4. Post-Loop Context Update

After the loop completes, run `morp finalize` to populate this file and the other
`.claude/` context files with real content.
